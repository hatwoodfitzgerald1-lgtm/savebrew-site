---
// The Today view (Offering Spec 10.3, screen 1). <ProductToday /> for the desktop window, <ProductToday phone /> for the phone view.
import ProductView from './ProductView.astro';
interface Props { phone?: boolean; page?: boolean; class?: string; id?: string; label?: string; interactive?: boolean; }
---
<ProductView view="today" {...Astro.props} />
